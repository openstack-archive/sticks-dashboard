============
Installation
============

At the command line::

    $ pip install sticks-dashboard

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sticks-dashboard
    $ pip install sticks-dashboard
